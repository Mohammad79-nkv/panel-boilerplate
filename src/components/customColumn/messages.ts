const key = 'TableFilter';

const messages = {
  fromDate: `${key}.fromDate`,
  toDate: `${key}.toDate`,
  confirm: `${key}.confirm`,
  dismiss: `${key}.dismiss`,
  phoneNumberError: `${key}.phoneNumberError`,
};

export default messages;
